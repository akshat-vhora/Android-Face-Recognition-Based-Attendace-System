<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/black_win</item>
    </style>

    <!-- Splash Screen Theme (No Action Bar) -->
    <style name="Theme.SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/black_win</item> <!-- Splash background color -->
    </style>

    <style name="TextInputLayoutBlackHint" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="hintTextColor">@color/black</item>
    </style>
</resources>